## 2026-04-13 - Cache Invalidation Trait for Frontend Performance\n**Learning:** Implementing cache invalidation via Eloquent Traits ( and ) is a clean and reusable way to manage cache keys tied to multiple models without cluttering the models themselves.\n**Action:** Always use Trait boot methods for cache invalidation when multiple models share the same cache key (e.g., a consolidated home page data payload).
## 2026-04-13 - Cache Invalidation Trait for Frontend Performance\n**Learning:** Implementing cache invalidation via Eloquent Traits (static::saved and static::deleted) is a clean and reusable way to manage cache keys tied to multiple models without cluttering the models themselves.\n**Action:** Always use Trait boot methods for cache invalidation when multiple models share the same cache key (e.g., a consolidated home page data payload).
